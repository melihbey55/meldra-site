services:
  - type: web
    name: meldra-site
    env: python
    plan: free
    pythonVersion: 3.11.9       # <-- BURADA Python sürümünü belirt
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
